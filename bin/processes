#!/bin/bash

SAMPLE_DIR="../samples"
SAMPLE_LOG="processes-samples.txt"
INTERVAL=10
COMMAND="ps aux --sort=-pcpu | ./ts '%Y-%m-%dT%H:%M:%S.%.S%z'"

mkdir -p $SAMPLE_DIR

while true; do
  echo $COMMAND | bash >> $SAMPLE_DIR/$SAMPLE_LOG
  sleep $SLEEP_TIME
done
